═══════════════════════════════════════════════════════════════
                    FinalPOS - IMPORTANT NOTES
═══════════════════════════════════════════════════════════════

⚠️ ABOUT .NET FRAMEWORK:
─────────────────────────
You DO NOT need Visual Studio to run this application!

.NET Framework is a Windows component (like Windows Media Player).
It's usually ALREADY INSTALLED on Windows 10/11.

You only need to download it if:
- You get an error saying ".NET Framework not found"
- You're using very old Windows version

Download link (only if needed):
https://dotnet.microsoft.com/download/dotnet-framework/net472

═══════════════════════════════════════════════════════════════

✅ WHAT YOU NEED:
─────────────────
1. MySQL (XAMPP is easiest - already installed on your PC)
2. Windows 7 or higher
3. .NET Framework (usually pre-installed)

═══════════════════════════════════════════════════════════════

🚀 HOW TO RUN:
──────────────

Option 1 - Easy Way:
1. Make sure XAMPP MySQL is running (XAMPP Control Panel)
2. Double-click: START_FinalPOS.bat

Option 2 - Direct:
1. Make sure XAMPP MySQL is running
2. Double-click: FinalPOS.exe

Option 3 - Install First:
1. Run: INSTALL.bat (creates desktop shortcut)
2. Then run FinalPOS.exe or use shortcut

═══════════════════════════════════════════════════════════════

📋 FIRST TIME SETUP:
────────────────────

1. Start XAMPP MySQL:
   - Open XAMPP Control Panel
   - Click "Start" next to MySQL
   - Wait until it turns green

2. Run FinalPOS:
   - Double-click START_FinalPOS.bat or FinalPOS.exe

3. If password prompt appears:
   - Enter your MySQL root password
   - Or leave blank if no password
   - Password will be saved for future use

4. Login:
   - Username: admin
   - Password: admin
   - ⚠️ Change password after first login!

═══════════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING:
───────────────────

Problem: "MySQL not found"
Solution: Start MySQL from XAMPP Control Panel

Problem: "Database initialization failed"
Solution: 
- Make sure XAMPP MySQL is running (green in XAMPP Control Panel)
- Check if MySQL is listening on port 3306
- Enter correct MySQL password if prompted

Problem: ".NET Framework not found"
Solution: 
- Usually already installed on Windows 10/11
- If error appears, download from Microsoft
- Link: https://dotnet.microsoft.com/download/dotnet-framework/net472

Problem: "Missing DLL files"
Solution:
- Make sure ALL files are in the same folder
- Don't delete any files
- Extract entire zip contents

═══════════════════════════════════════════════════════════════

📝 QUICK CHECKLIST:
───────────────────

☐ XAMPP MySQL is running (check XAMPP Control Panel)
☐ All files extracted to same folder
☐ Run START_FinalPOS.bat or FinalPOS.exe
☐ Enter MySQL password if asked
☐ Login with admin/admin

═══════════════════════════════════════════════════════════════

That's it! No Visual Studio needed! 🎉

